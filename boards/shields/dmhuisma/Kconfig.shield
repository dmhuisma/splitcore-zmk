# No whitespace after the comma or in your part name!
config SHIELD_SPLITCORE_LEFT
    def_bool $(shields_list_contains,splitcore_left)

# No whitespace after the comma or in your part name!
config SHIELD_SPLITCORE_RIGHT
    def_bool $(shields_list_contains,splitcore_right)
